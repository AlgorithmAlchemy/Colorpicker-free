# 🚀 Запуск ColorPicker

## Быстрый запуск

### Windows
```bash
# Двойной клик на start.bat
# Или в командной строке:
python run.py
```

### Linux/macOS
```bash
python run.py
```

## Если возникают проблемы

### 1. Проверка зависимостей
```bash
python fix_qt.py
```

### 2. Ручная установка
```bash
pip install -r requirements.txt
```

### 3. Совместимость с NumPy
Если возникает ошибка с NumPy 2.x:
```bash
pip install "numpy<2.0.0"
pip install PySide6 pyautogui opencv-python
```

## Использование

- **Перемещение курсора** - координаты и цвет обновляются в реальном времени
- **CTRL** - захват цвета под курсором
- **Кнопка "CTRL - Захватить цвет"** - альтернативный способ захвата
- **ESC** - выход из приложения
- **Перетаскивание окна** - зажмите левую кнопку мыши и перетащите

## Альтернативный запуск

Для запуска основного модуля приложения:
```bash
python -m app
```

С параметрами:
```bash
python -m app --light-theme  # Светлая тема
python -m app --alpha        # С поддержкой альфа-канала
```
