#!/usr/bin/env python3
"""
Скрипт для установки библиотеки keyboard для глобальных горячих клавиш.
"""

import sys
import subprocess


def install_keyboard():
    """Устанавливает библиотеку keyboard."""
    print("TOOL Установка библиотеки keyboard для глобальных горячих клавиш...")
    print("=" * 60)
    
    try:
        # Установлена ли библиотека
        import keyboard
        print("OK keyboard уже установлен")
        print("🌐 Глобальные горячие клавиши будут работать!")
        return True
    except ImportError:
        print("WARNING  keyboard не найден. Устанавливаем...")
    
    try:
        # Keyboard
        subprocess.run([
            sys.executable, "-m", "pip", "install", "keyboard>=0.13.5"
        ], check=True)
        
        print("OK keyboard успешно установлен!")
        print("🌐 Теперь глобальные горячие клавиши будут работать в играх и других приложениях")
        
        # установку
        import keyboard
        print("OK keyboard успешно импортирован")
        
        return True
        
    except subprocess.CalledProcessError as e:
        print(f"ERROR Ошибка установки keyboard: {e}")
        print("\nTIP Попробуйте установить вручную:")
        print("   pip install keyboard>=0.13.5")
        return False
    except Exception as e:
        print(f"ERROR Неожиданная ошибка: {e}")
        return False


def main():
    """Основная функция."""
    print("COLOR Desktop Color Picker - Установка глобальных горячих клавиш")
    print("=" * 60)
    
    if install_keyboard():
        print("\n🎉 Установка завершена успешно!")
        print("📋 Теперь запустите приложение:")
        print("   python run.py")
        print("   или")
        print("   python run_improved.py")
        print("\n🌐 Глобальные горячие клавиши будут работать в:")
        print("   - Играх")
        print("   - Других приложениях")
        print("   - Когда окно colorpicker не активно")
    else:
        print("\nERROR Установка не удалась.")
        print("TIP Приложение будет работать, но только с локальными горячими клавишами")
        print("   (когда окно активно)")


if __name__ == "__main__":
    main()
