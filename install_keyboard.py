#!/usr/bin/env python3
"""
Скрипт для установки библиотеки keyboard для глобальных горячих клавиш.
"""

import sys
import subprocess


def install_keyboard():
    """Устанавливает библиотеку keyboard."""
    print("🔧 Установка библиотеки keyboard для глобальных горячих клавиш...")
    print("=" * 60)
    
    try:
        # Проверяем, установлена ли уже библиотека
        import keyboard
        print("✅ keyboard уже установлен")
        print("🌐 Глобальные горячие клавиши будут работать!")
        return True
    except ImportError:
        print("⚠️  keyboard не найден. Устанавливаем...")
    
    try:
        # Устанавливаем keyboard
        subprocess.run([
            sys.executable, "-m", "pip", "install", "keyboard>=0.13.5"
        ], check=True)
        
        print("✅ keyboard успешно установлен!")
        print("🌐 Теперь глобальные горячие клавиши будут работать в играх и других приложениях")
        
        # Проверяем установку
        import keyboard
        print("✅ keyboard успешно импортирован")
        
        return True
        
    except subprocess.CalledProcessError as e:
        print(f"❌ Ошибка установки keyboard: {e}")
        print("\n💡 Попробуйте установить вручную:")
        print("   pip install keyboard>=0.13.5")
        return False
    except Exception as e:
        print(f"❌ Неожиданная ошибка: {e}")
        return False


def main():
    """Основная функция."""
    print("🎨 Desktop Color Picker - Установка глобальных горячих клавиш")
    print("=" * 60)
    
    if install_keyboard():
        print("\n🎉 Установка завершена успешно!")
        print("📋 Теперь запустите приложение:")
        print("   python run.py")
        print("   или")
        print("   python run_improved.py")
        print("\n🌐 Глобальные горячие клавиши будут работать в:")
        print("   - Играх")
        print("   - Других приложениях")
        print("   - Когда окно colorpicker не активно")
    else:
        print("\n❌ Установка не удалась.")
        print("💡 Приложение будет работать, но только с локальными горячими клавишами")
        print("   (когда окно активно)")


if __name__ == "__main__":
    main()
