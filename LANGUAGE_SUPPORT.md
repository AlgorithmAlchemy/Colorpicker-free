# Система интернационализации

## Обзор

В приложение Color Picker добавлена полная поддержка интернационализации с возможностью переключения между 5 языками:

- 🇷🇺 **Русский** (по умолчанию)
- 🇺🇸 **English** 
- 🇩🇪 **Deutsch**
- 🇫🇷 **Français**
- 🇪🇸 **Español**

## Функциональность

### Автоматическое переключение языка
- Язык сохраняется в настройках и восстанавливается при следующем запуске
- Все элементы интерфейса автоматически обновляются при смене языка
- Поддержка контекстного меню на всех языках

### Переведенные элементы
- Заголовок окна
- Кнопки (OK, Отмена, Копировать)
- Контекстное меню
- Названия цветовых каналов (RGB, HSV, Alpha)
- Настройки и темы
- Сообщения об ошибках

## Использование

### Переключение языка через контекстное меню
1. Щелкните правой кнопкой мыши на окне приложения
2. Выберите пункт "🌐 Язык"
3. Выберите нужный язык из списка
4. Интерфейс автоматически обновится

### Программное переключение языка
```python
from app.i18n import set_language, Language

# Установить английский язык
set_language(Language.ENGLISH)

# Установить немецкий язык
set_language(Language.GERMAN)
```

### Получение переведенного текста
```python
from app.i18n import get_text

# Получить переведенный текст
title = get_text("window_title")
ok_text = get_text("ok")
```

## Структура файлов

### Основные файлы
- `app/i18n.py` - Система интернационализации
- `app/ui_updater.py` - Обновление интерфейса
- `app/ui/context_menu.py` - Контекстное меню с поддержкой языков
- `app/colorpicker.py` - Основное окно с переводами

### Добавленные переводы
Все переводы находятся в файле `app/i18n.py` в словаре `TRANSLATIONS`:

```python
TRANSLATIONS = {
    "window_title": {
        "ru": "Выбор цвета",
        "en": "Color Picker",
        "de": "Farbauswahl",
        "fr": "Sélecteur de couleur",
        "es": "Selector de color"
    },
    # ... другие переводы
}
```

## Тестирование

Для проверки работы системы интернационализации запустите:

```bash
python test_language.py
```

Этот скрипт покажет все переводы для каждого языка.

## Добавление новых языков

### 1. Добавить новый язык в enum
```python
class Language(Enum):
    RUSSIAN = "ru"
    ENGLISH = "en"
    GERMAN = "de"
    FRENCH = "fr"
    SPANISH = "es"
    NEW_LANGUAGE = "xx"  # Добавить новый язык
```

### 2. Добавить название языка
```python
LANGUAGE_NAMES = {
    Language.RUSSIAN: "Русский",
    # ... другие языки
    Language.NEW_LANGUAGE: "Название языка"
}
```

### 3. Добавить переводы
```python
TRANSLATIONS = {
    "key": {
        "ru": "Русский текст",
        "en": "English text",
        # ... другие языки
        "xx": "Перевод на новый язык"
    }
}
```

### 4. Добавить в контекстное меню
В файле `app/ui/context_menu.py` добавить новый язык в список:

```python
languages = [
    ("ru", "🇷🇺"),
    ("en", "🇺🇸"),
    # ... другие языки
    ("xx", "🏳️")  # Добавить новый язык
]
```

## Сохранение настроек

Язык автоматически сохраняется в настройках приложения и восстанавливается при следующем запуске. Настройки хранятся в базе данных SQLite.

## Совместимость

Система интернационализации полностью совместима с существующим кодом и не нарушает работу приложения. Все изменения обратно совместимы.
