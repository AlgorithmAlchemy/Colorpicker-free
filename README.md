# vcolorpicker

Простой и удобный цветовой пикер с графическим интерфейсом для Python.

## Возможности

- 🎨 Интерактивный выбор цвета через графический интерфейс
- 🔄 Поддержка различных цветовых форматов (RGB, HSV, HEX)
- 🌈 Поддержка альфа-канала (прозрачности)
- 🌓 Светлая и темная темы интерфейса
- 📱 Современный и отзывчивый дизайн
- 🔧 Простой и понятный API
- 🧪 Полное покрытие тестами
- 📚 Подробная документация

## Установка

```bash
pip install vcolorpicker
```

## Быстрый старт

### Простое использование

```python
from vcolorpicker import get_color

# Открыть цветовой пикер
color = get_color()
print(f"Выбранный цвет: {color}")  # (255, 128, 0)

# С начальным цветом
color = get_color((255, 0, 0))
print(f"Выбранный цвет: {color}")  # (255, 0, 0)
```

### Использование с альфа-каналом

```python
from vcolorpicker import get_color, use_alpha

# Включить поддержку альфа-канала
use_alpha(True)

# Выбрать цвет с прозрачностью
color = get_color((255, 0, 0, 50))
print(f"Цвет с альфа: {color}")  # (255, 128, 0, 75)
```

### Использование светлой темы

```python
from vcolorpicker import get_color, use_light_theme

# Включить светлую тему
use_light_theme(True)

# Открыть пикер
color = get_color()
```

## Подробное использование

### Создание экземпляра ColorPicker

```python
from vcolorpicker import ColorPicker

# Создать пикер с настройками
picker = ColorPicker(
    light_theme=True,  # светлая тема
    use_alpha=True     # поддержка альфа-канала
)

# Получить цвет
color = picker.get_color((255, 0, 0, 50))
```

### Конвертация цветов

```python
from vcolorpicker import hsv2rgb, rgb2hsv, rgb2hex, hex2rgb

# RGB в HSV
hsv = rgb2hsv(255, 0, 0)  # (0, 100, 100)

# HSV в RGB
rgb = hsv2rgb(0, 100, 100)  # (255, 0, 0)

# RGB в HEX
hex_color = rgb2hex(255, 0, 0)  # "ff0000"

# HEX в RGB
rgb = hex2rgb("ff0000")  # (255, 0, 0)
```

### Конфигурация

```python
from vcolorpicker import get_config, set_config, ColorPickerConfig

# Получить текущую конфигурацию
config = get_config()
print(f"Светлая тема: {config.light_theme}")
print(f"Альфа-канал: {config.use_alpha}")

# Установить новую конфигурацию
new_config = ColorPickerConfig(light_theme=True, use_alpha=True)
set_config(new_config)
```

## API Справочник

### Основные функции

#### `get_color(initial_color=None)`

Показывает цветовой пикер и возвращает выбранный цвет.

**Параметры:**
- `initial_color` (tuple, optional): Начальный цвет для отображения

**Возвращает:**
- `tuple`: Выбранный цвет в формате RGB или RGBA

#### `ColorPicker(light_theme=False, use_alpha=False)`

Создает экземпляр цветового пикера.

**Параметры:**
- `light_theme` (bool): Использовать светлую тему
- `use_alpha` (bool): Включить поддержку альфа-канала

### Утилиты для работы с цветами

#### `hsv2rgb(h_or_color, s=0, v=0, a=None)`

Конвертирует HSV цвет в RGB.

#### `rgb2hsv(r_or_color, g=0, b=0, a=None)`

Конвертирует RGB цвет в HSV.

#### `rgb2hex(r_or_color, g=0, b=0, a=0)`

Конвертирует RGB цвет в HEX.

#### `hex2rgb(hex_color)`

Конвертирует HEX цвет в RGB.

#### `hex2hsv(hex_color)`

Конвертирует HEX цвет в HSV.

#### `hsv2hex(h_or_color, s=0, v=0, a=0)`

Конвертирует HSV цвет в HEX.

### Конфигурация

#### `use_light_theme(value=True)`

Устанавливает использование светлой темы.

#### `use_alpha(value=True)`

Устанавливает использование альфа-канала.

#### `get_config()`

Возвращает текущую конфигурацию.

#### `set_config(config)`

Устанавливает новую конфигурацию.

## Примеры

### Пример 1: Простой выбор цвета

```python
from vcolorpicker import get_color

def choose_background_color():
    """Позволяет пользователю выбрать цвет фона."""
    print("Выберите цвет фона:")
    color = get_color()
    
    if color:
        print(f"Выбранный цвет фона: RGB{color}")
        return color
    else:
        print("Выбор цвета отменен")
        return None

# Использование
bg_color = choose_background_color()
```

### Пример 2: Выбор цвета с прозрачностью

```python
from vcolorpicker import get_color, use_alpha

def choose_overlay_color():
    """Позволяет пользователю выбрать цвет наложения с прозрачностью."""
    use_alpha(True)
    
    print("Выберите цвет наложения:")
    color = get_color((255, 255, 255, 50))  # полупрозрачный белый
    
    if color and len(color) == 4:
        r, g, b, a = color
        print(f"Выбранный цвет: RGB({r}, {g}, {b}) с прозрачностью {a}%")
        return color
    else:
        print("Выбор цвета отменен")
        return None
```

### Пример 3: Конвертация цветовых форматов

```python
from vcolorpicker import hsv2rgb, rgb2hsv, rgb2hex, hex2rgb

def demonstrate_color_conversion():
    """Демонстрирует конвертацию между цветовыми форматами."""
    
    # Начальный RGB цвет
    rgb_color = (255, 128, 0)
    print(f"RGB цвет: {rgb_color}")
    
    # Конвертация в HSV
    hsv_color = rgb2hsv(rgb_color)
    print(f"HSV цвет: {hsv_color}")
    
    # Конвертация в HEX
    hex_color = rgb2hex(rgb_color)
    print(f"HEX цвет: #{hex_color}")
    
    # Обратная конвертация
    rgb_back = hex2rgb(hex_color)
    print(f"RGB обратно: {rgb_back}")
    
    # Проверка корректности
    assert rgb_color == rgb_back
    print("Конвертация корректна!")

demonstrate_color_conversion()
```

### Пример 4: Настройка темы

```python
from vcolorpicker import get_color, use_light_theme, use_alpha

def choose_color_with_theme(use_light=True, use_alpha_channel=True):
    """Выбор цвета с настраиваемой темой и альфа-каналом."""
    
    # Настройка темы
    use_light_theme(use_light)
    use_alpha(use_alpha_channel)
    
    # Выбор цвета
    theme_name = "светлой" if use_light else "темной"
    alpha_text = " с альфа-каналом" if use_alpha_channel else ""
    
    print(f"Выберите цвет ({theme_name} тема{alpha_text}):")
    color = get_color()
    
    if color:
        print(f"Выбранный цвет: {color}")
        return color
    else:
        print("Выбор цвета отменен")
        return None

# Примеры использования
color1 = choose_color_with_theme(use_light=True, use_alpha_channel=False)
color2 = choose_color_with_theme(use_light=False, use_alpha_channel=True)
```

## Разработка

### Установка для разработки

```bash
git clone https://github.com/nlfmt/vcolorpicker.git
cd vcolorpicker
pip install -e .
```

### Запуск тестов

```bash
pytest tests/
```

### Структура проекта

```
vcolorpicker/
├── __init__.py          # Основной модуль
├── types.py             # Типы данных
├── color_utils.py       # Утилиты для работы с цветами
├── config.py            # Конфигурация
├── picker.py            # Основной класс ColorPicker
├── facade.py            # Фасадный API
├── exceptions.py        # Пользовательские исключения
├── validators.py        # Валидаторы
└── ui/                  # UI файлы
    ├── ui_dark.ui
    ├── ui_light.ui
    └── ...

tests/
├── __init__.py
├── test_color_utils.py
└── ...

example.py               # Примеры использования
README.md               # Документация
```

## Изменения в версии 2.0.0

### Новые возможности

- ✅ Полная типизация с type hints
- ✅ Модульная архитектура
- ✅ Улучшенная обработка ошибок
- ✅ Валидация входных данных
- ✅ Подробная документация
- ✅ Полное покрытие тестами
- ✅ Современный API

### Обратная совместимость

Все функции из версии 1.x остаются доступными:

```python
# Старый API (все еще работает)
from vcolorpicker import getColor, useAlpha, useLightTheme
color = getColor((255, 0, 0))

# Новый API (рекомендуется)
from vcolorpicker import get_color, use_alpha, use_light_theme
color = get_color((255, 0, 0))
```

## Лицензия

MIT License - см. файл [LICENSE](LICENSE) для подробностей.

## Автор

nlfmt

## Поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории проекта.

